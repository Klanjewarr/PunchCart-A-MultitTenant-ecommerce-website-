/**
 * Home page component that renders a simple div with the text "Home Page".
 *
 * Renders a single root `<div>` containing the literal text "Home Page". This component accepts no props and has no side effects.
 *
 * @returns The component's JSX element.
 */
export default  function Home() {

 return(
 <div>
  Home Page
 </div>
 );
}
